import React from 'react'
import ReactDOM from 'react-dom/client'
import CoveredCallDashboard from './Dashboard'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CoveredCallDashboard />
  </React.StrictMode>,
)
